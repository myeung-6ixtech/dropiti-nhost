# Nhost CLI: real secrets at repo root (never commit)
.secrets

functions/node_modules/
functions/dist/

# Optional scratch next to committed templates in secrets/
secrets/*.local
secrets/local.secrets
secrets/.env

